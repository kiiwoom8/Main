% \draw (el) to[circle connection boomerang switch color=from (beige) to (black) by 40] (entropy);
\draw[-stealth, shorten >=10pt,shorten <=10pt, line width=5pt, color=gray, postaction={decorate,decoration={text along path, text align=center, raise=.8cm, text={requires energy}}}](entropy) to[bend left=45]  (el);